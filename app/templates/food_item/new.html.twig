{# templates/food_item/new.html.twig #}
{% extends 'base.html.twig' %}

{% block title %}New Food Item{% endblock %}

{% block body %}
<div class="container text-center mt-5">
    <h1 class="mb-4">New Food Item</h1>

    <div class="d-flex justify-content-center">
        <div class="w-50">
            {# include the _form partial, which now renders everything with one line #}
            {{ include('food_item/_form.html.twig') }}

            <div class="mt-3">
                <a href="{{ path('app_home') }}" class="btn btn-secondary btn-lg">
                    ← Back
                </a>
            </div>
        </div>
    </div>
</div>
{% endblock %}
