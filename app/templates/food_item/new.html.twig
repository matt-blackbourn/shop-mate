{% extends 'base.html.twig' %}

{% block title %}New Food Item{% endblock %}

{% block body %}
<header>
    <h1 class="text-center">New Food Item</h1>
</header>

<main>
    <div class="container">
        {{ include('food_item/_form.html.twig') }}

        <div class="mt-3">
            <a href="{{ path('app_home') }}" class="btn btn-secondary btn-lg w-100">
                Home
            </a>
        </div>
    </div>
</main>
{% endblock %}
